services:
  db:
    image: ysql:8.0.41
    container_name: <container_name>
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: <password>
      MYSQL_DATABASE: <db_name>
    ports:
      - "<port>:3306"
    volumes:
      - <container_name>:/var/lib/mysql
   
  redis-cache:
    image: redis:alpine
    container_name: <container_name>
    restart: always
    ports:
      - "6379:6379"
  
   rabbitmq:
    image: rabbitmq:3-management
    container_name: <container_name>
    restart: always
    ports:
      - "5672:5672"  
      - "15672:15672" 
    environment:
      RABBITMQ_DEFAULT_USER: myusername
      RABBITMQ_DEFAULT_PASS: mypassword
      RABBITMQ_DEFAULT_VHOST: myvhost
    volumes:
      - <container_name>:/var/lib/rabbitmq
 
volumes:
  <sql_container_name>:
  <rabbitmq_container_name>:
